# Copy to .env and point at your GCP service account JSON (gitignored).
# Create key: GCP Console → IAM → Service Accounts → Keys → Add key (JSON)
# Roles needed: BigQuery Data Viewer, BigQuery Job User (project arrowsmaster-6b84f)

GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/arrows-bi-readonly.json
